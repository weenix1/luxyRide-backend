syntax = "proto3";

package driver;

service DriverService {
  rpc GetDriverInfo (DriverRequest) returns (DriverResponse);
}

message DriverRequest {
  string id = 1;
}

message DriverResponse {
  string id = 1;
  string name = 2;
}
